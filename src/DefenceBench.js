import bench from "./imgs/courtroom/background-defense bench(gba).gif"

const DefenceBench = () => {
    return <img src={bench} style={{position:"relative", zIndex:3, transform:"scale(1.28)", right: 228, bottom: 3}}></img> 
}

export default DefenceBench