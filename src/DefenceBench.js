import bench from "./imgs/courtroom/background-defense bench(gba).gif"

const DefenceBench = () => {
    return <img src={bench} style={{position:"absolute", zIndex:3, transform:"scale(1.28)", marginLeft:28, top: 161}}></img> 
}

export default DefenceBench